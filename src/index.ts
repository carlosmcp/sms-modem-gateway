/* import { GatewayModemSMS, STATUS } from './GatewayModemSMS'

(() => {
  const number = '11-98888888'
  const message = 'Alows!'

  new GatewayModemSMS()
    .sendMessage(number, message)
    .then(result => expect(result).toBe(STATUS.SEND_OK))
    .catch(e => console.error(e))
})()
 */
